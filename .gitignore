# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual envs / conda
.venv/
env/
venv/

# Model checkpoints & weights — mount at runtime, do not commit
*.pt
*.pth
*.ckpt
*.safetensors
*.bin
*.onnx
hf_cache/
.insightface/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Temp / job outputs
*.mp4
*.wav
*.png
*.jpg
# … but keep the lip-mask assets shipped with the LatentSync repo — `load_fixed_mask`
# reads these at inference time and the container fails fast without them.
!latentsync/utils/mask*.png

# Secrets / env
.env
.env.*
